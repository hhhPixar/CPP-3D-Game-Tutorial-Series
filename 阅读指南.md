# 阅读指南 · DX3D 3D 游戏引擎教程

> 本指南写给**只有一点点编程基础**、想读懂这个项目的读者。
> 目标：让你在看代码之前先建立「地图」，知道每个文件做什么、为什么这么写、按什么顺序读。

---

## 1. 这个项目是什么

`CPP-3D-Game Tutorial Series` 是一套从零开始的 C++ 3D 游戏开发教程。本仓库包含两部分：

| 部分 | 目录 | 作用 |
|------|------|------|
| **DX3D 引擎** | `DX3D/` | 一个自研的 3D 游戏引擎（底层库），别人拿它就能做游戏 |
| **游戏示例** | `Game/` | 用 DX3D 引擎搭出来的一个小 3D 场景（立方体台座 + 大理石半身像 + 旋转方向光 + 第一人称玩家） |

**技术栈**：
- 语言：**C++20**（用到了模板、`concept`/`requires`、`std::span`、`std::views`、`std::format` 等现代特性）
- 图形 API：**Direct3D 11**（DirectX 11，Windows 平台的 GPU 图形接口）
- 窗口/输入：**Win32 API**（Windows 原生窗口与消息系统）
- 着色器语言：**HLSL**（High-Level Shading Language，DirectX 的着色器语言）

> 注意：这个项目只能在 **Windows + Visual Studio 2022** 下编译运行（因为用了 DirectX 和 Win32）。

---

## 2. 读代码前：先知道这些概念（扫盲）

如果下面这些词你完全陌生，先花 10 分钟读完本节，否则后面会处处卡壳。

### 2.1「引擎」和「游戏」的关系
- **引擎**是可复用的底层库（窗口、图形、输入、资源管理、实体系统）。
- **游戏**是具体内容（这个场景里放什么物体、玩家怎么操作）。
- 类比：引擎 = 汽车底盘与发动机；游戏 = 装上什么座椅、涂什么颜色、开去哪里。

### 2.2 C++ 基础（你只需要懂到这一步）
- **头文件 `.h` 与源文件 `.cpp`**：`.h` 声明「有什么」（类、函数原型），`.cpp` 实现「怎么做」。别人 `#include` 你的 `.h` 就能用你的代码。
- **命名空间 `namespace dx3d`**：引擎所有代码包在 `dx3d::` 里，避免和别人重名。
- **类、成员变量、成员函数**：成员变量前缀 `m_`（如 `m_logger`）是本项目惯例，表示「成员（member）」。
- **指针 / 引用 / 智能指针**：
  - `T&` 是引用（别名，不持有）。
  - `std::unique_ptr<T>`（本项目叫 `UniquePtr<T>`）：独占所有权，离开作用域自动释放。
  - `std::shared_ptr<T>`（本项目叫 `RefPtr<T>`）：共享所有权，引用计数归零才释放。
- **模板 `template<typename T>`**：写一份代码给多种类型用，如 `createOrGetComponent<CameraComponent>()`。
- **`concept` / `requires`**（C++20）：编译期约束。`requires IsRegistered<Component, T>` 意思是「只有注册过的类型 T 才允许调用」，写错了编译就报错。

### 2.3 DirectX / Direct3D 11 是什么
- **DirectX** 是微软的一套多媒体 API，**Direct3D（D3D）** 是其中负责 3D 图形的部分。
- D3D11 的核心对象：
  - `ID3D11Device`（**设备**）：创建 GPU 资源的工厂（建缓冲区、纹理、着色器……）。
  - `ID3D11DeviceContext`（**设备上下文**）：真正下命令画图的（设缓冲、画三角形、提交）。
  - `IDXGISwapChain`（**交换链**）：管理前台/后台两张图，画完后「交换」呈现到屏幕（双缓冲，避免画面撕裂）。
- **COM 与 `ComPtr`**：DirectX 对象是 COM 组件，用 `Microsoft::WRL::ComPtr<T>` 这种智能指针管理，自动计数、自动释放。

### 2.4 图形渲染管线（一句话：数据怎么变成屏幕上的像素）
顶点数据 → **顶点着色器(Vertex Shader)** 把顶点变换到屏幕空间 → 组装三角形 → **光栅化** → **像素着色器(Pixel Shader)** 给每个像素上色 → 写入后台缓冲 → 交换呈现。
本项目里：
- 顶点位置用 **仿射世界矩阵 `affineWorld`**（含缩放）变换。
- 顶点法线用 **刚体世界矩阵 `rigidWorld`**（不含缩放）变换，避免缩放把法线扭曲导致光照错误。
- 然后再依次乘 **观察矩阵 view**、**投影矩阵 proj** 得到屏幕坐标。

### 2.5 ECS（实体-组件-系统，Entity-Component-System）
这是游戏里组织物体的方式：
- **Entity / GameObject**：一个「东西」，本身啥也没有，就是个容器。
- **Component**：挂上去的能力（Transform 变换、Camera 相机、Mesh 网格、Light 光……）。
- **System**：遍历所有某类组件做事情（本项目的 `WorldRenderer` 就是遍历相机/光照/网格组件来画图）。
- 好处：灵活组合，一个物体想要相机就挂 CameraComponent，想要网格就挂 MeshComponent。

### 2.6 一点点数学
- **向量 Vec3**：有方向的量，表示位置/方向/颜色。
- **矩阵 Mat4x4**：4×4 的数表，用来做空间变换（平移、旋转、缩放、投影）。点/向量乘矩阵 = 变换到新空间。
- **左手坐标系 LH**：DirectX 默认左手系（+X 右、+Y 上、+Z 朝屏幕里）。`orthoLH`/`perspectiveFovLH` 的 LH 就是这个意思。
- **归一化 normalize**：把向量长度变成 1，常用于方向。

---

## 3. 项目目录结构

```
.
├─ Game/                      # 游戏示例（最易读，从这里开始）
│  ├─ main.cpp                # 程序入口
│  ├─ MainGame.h/.cpp         # 游戏主类，搭建场景
│  ├─ Objects/Player.h/.cpp   # 玩家（第一人称相机控制）
│  └─ Assets/                 # 游戏资源：贴图、网格(.obj)、材质着色器(.hlsl)
│
├─ DX3D/                      # 引擎
│  ├─ Include/DX3D/           # 引擎公共 API（头文件 .h，对外暴露的接口）
│  │  ├─ Core/                # 地基：类型、日志、基类、反射宏
│  │  ├─ Math/                # 数学库：Vec2/3/4、Rect、Mat4x4
│  │  ├─ Window/              # 窗口抽象
│  │  ├─ Input/               # 输入系统
│  │  ├─ Game/                # 游戏框架 + ECS（Game/World/GameObject/Component/Display）
│  │  ├─ Component/           # 具体组件（Transform/Camera/Cube/Mesh/DirectionalLight）
│  │  └─ Resource/            # 资源系统（Resource/ResourceManager/Mesh/Texture/Material）
│  │
│  ├─ Source/DX3D/            # 引擎实现（.cpp）
│  │  ├─ Core/ Math/ Graphics/ Window/ Input/ Game/ Component/ Resource/
│  │  └─ Graphics/            # Direct3D 11 图形引擎（设备、上下文、缓冲、纹理、管线…）
│  │
│  ├─ Assets/Shaders/         # 引擎公共着色器 Common.hlsl
│  └─ Vendor/                 # 第三方库（stb_image 读图、tinyobjloader 读 .obj）—— 不用读
│
├─ DirectXGame.sln            # Visual Studio 解决方案（用它打开编译）
└─ 阅读指南.md                 # ← 你正在读的这个文件
```

> 规律：`Include/DX3D/Xxx/XxxClass.h` 声明，`Source/DX3D/Xxx/XxxClass.cpp` 实现。头文件分目录（Core/Math/Game/Component/Resource），但**图形相关头文件**统一放在 `Source/DX3D/Graphics/`（它们是对内的实现细节，不作为公共 API 暴露）。

---

## 4. 架构总览

### 4.1 启动流程
```
main()                                    Game/main.cpp
  └─ try { MainGame(desc).run(); }       创建游戏、进入主循环
        └─ MainGame 构造 → 委托给父类 dx3d::Game 构造
              └─ dx3d::Game 构造函数里依次创建所有子系统：  DX3D/Source/DX3D/Game/Game.cpp
                    Logger → InputSystem → GraphicsDevice
                    → Display(=Window+SwapChain) → ResourceManager
                    → World → WorldRenderer
        └─ onCreate()  ← 子类在这里搭建场景（放台座、半身像、灯、玩家）
        └─ run()      ← 进入消息循环，每帧调用 onInternalUpdate()
```

### 4.2 一帧（frame）发生了什么
`Game::onInternalUpdate()`（`Game.cpp`）每帧做：
1. 用 `std::chrono::steady_clock` 算出上一帧到现在的间隔 **deltaTime**（秒）。
2. `inputSystem->update()` —— 采集本帧键盘/鼠标状态。
3. `onUpdate(deltaTime)` —— 调用子类的游戏逻辑（玩家移动、灯旋转）。
4. `world->update(deltaTime)` —— 更新 ECS：处理待创建对象、批量更新脏 Transform。
5. `worldRenderer->render(...)` —— **把世界画出来**：清屏 → 设置相机/光照/物体数据 → 画三角形 → 提交命令 → present 呈现。

### 4.3 子系统关系
```
                 ┌──────────── dx3d::Game ────────────┐
                 │ 持有并协调下面所有子系统              │
                 └────────────────────────────────────┘
   Logger ──────────── 日志（所有对象都能用 getLogger()）
   InputSystem ─────── 键盘/鼠标（Player 用它做第一人称控制）
   GraphicsDevice ───── D3D11 设备，创建一切 GPU 资源的工厂
   Display ─────────── Window + SwapChain（窗口 + 交换链）
   ResourceManager ─── 从文件加载 Mesh(.obj)/Texture(图片)/Material(.hlsl)，带缓存去重
   World ───────────── ECS 容器：所有 GameObject + Component
   WorldRenderer ────── 遍历 World 里的组件，发出 D3D 绘制命令
        │
        ▼ 每帧调用 GraphicsDevice / DeviceContext / SwapChain 把图画到屏幕
```

---

## 5. 推荐阅读顺序（从易到难、从外到内）

> 建议严格按这个顺序读。每个文件现在都已有详细中文注释，读不懂就看注释。
> 一句话总结每个文件干什么，帮你判断是否读懂了。

### 阶段 0 · 入口与游戏示例（先读，建立全局观）
| 文件 | 读懂它你就知道了 |
|------|------|
| `Game/main.cpp` | 程序怎么启动、异常怎么兜底 |
| `Game/MainGame.h` + `.cpp` | 一个游戏场景怎么用引擎搭出来（台座/半身像/灯/玩家） |
| `Game/Objects/Player.h` + `.cpp` | 第一人称相机怎么用鼠标+WASD 移动 |

> 读完这 5 个文件，你就懂了「这个游戏在干什么」。后面的引擎细节是「它是怎么做到的」。

### 阶段 1 · 核心地基 Core
| 文件 | 读懂它你就知道了 |
|------|------|
| `DX3D/Include/DX3D/Core/Core.h` | 引擎的类型别名（i32/f32/RefPtr/UniquePtr）、禁拷贝宏 |
| `DX3D/Include/DX3D/Core/Base.h` + `Source/.../Core/Base.cpp` | 所有引擎对象的基类（持 Logger） |
| `DX3D/Include/DX3D/Core/Logger.h` + `Source/.../Core/Logger.cpp` | 日志系统与 DX3DLog* 宏 |
| `DX3D/Include/DX3D/Core/Identifiable.h` | typeid 宏与 IsRegistered concept（ECS 按类型找组件的关键） |
| `DX3D/Include/DX3D/Core/Common.h` | 所有 Desc 描述结构体集中在这里（引擎的「创建参数表」） |
| `DX3D/Include/DX3D/All.h` | 汇总头，include 它就能用整个引擎公共 API |

> 关键理解：引擎用「**Desc 结构体 + 构造函数**」统一创建对象，而不是传一堆零散参数。看 `Common.h` 里的 `*Desc` 就是各种创建参数表。

### 阶段 2 · 数学库 Math
| 文件 | 读懂它你就知道了 |
|------|------|
| `DX3D/Include/DX3D/Math/Vec2.h` | 二维向量（纹理坐标用） |
| `DX3D/Include/DX3D/Math/Vec3.h` | 三维向量（位置/方向/法线/颜色） |
| `DX3D/Include/DX3D/Math/Vec4.h` | 四维向量（含四元叉积，矩阵求逆用） |
| `DX3D/Include/DX3D/Math/Rect.h` | 尺寸/矩形（窗口、视口大小） |
| `DX3D/Include/DX3D/Math/Mat4x4.h` | 4×4 矩阵：平移/旋转/缩放/投影/求逆 |
| `DX3D/Include/DX3D/Math/MathUtils.h` | PI 等常量 |

> 重点：`Mat4x4.h` 里的 `translate/rotateX(Y)(Z)/scale/perspectiveFovLH`，每个矩阵的几何含义注释都写了。这是理解「3D 怎么变到屏幕」的钥匙。

### 阶段 3 · 窗口与输入
| 文件 | 读懂它你就知道了 |
|------|------|
| `DX3D/Include/DX3D/Window/Window.h` | 窗口抽象基类 |
| `DX3D/Source/DX3D/Window/Win32/Win32Window.cpp` | Win32 怎么建窗口、跑消息循环、处理按键鼠标消息 |
| `DX3D/Include/DX3D/Input/InputSystem.h` | 输入系统接口 |
| `DX3D/Source/DX3D/Input/InputSystem.cpp` | 按键状态/鼠标增量/鼠标锁定（FPS 视角用）的实现 |

### 阶段 4 · 图形引擎 Graphics（重点，分 3 小步读）
**4a · 设备与上下文（D3D11 的核心）**
| 文件 | 读懂它你就知道了 |
|------|------|
| `Source/DX3D/Graphics/GraphicsDevice.h` + `.cpp` | D3D11 设备：创建所有 GPU 资源的工厂 |
| `Source/DX3D/Graphics/DeviceContext.h` + `.cpp` | 真正下绘制命令的地方 |
| `Source/DX3D/Graphics/SwapChain.h` + `.cpp` | 交换链：双缓冲与 present |
| `Source/DX3D/Graphics/GraphicsResource.h` `GraphicsLogUtils.h` `GraphicsUtils.h` | GPU 资源基类、错误检查辅助 |

**4b · 管线与着色器**
| 文件 | 读懂它你就知道了 |
|------|------|
| `Source/DX3D/Graphics/ShaderBinary.h` + `.cpp` | 编译后的着色器字节码 |
| `Source/DX3D/Graphics/ShaderInclude.h` | 让 HLSL 的 #include 生效 |
| `Source/DX3D/Graphics/VertexShaderSignature.h` + `.cpp` | 顶点着色器输入签名 |
| `Source/DX3D/Graphics/GraphicsPipelineLayout.h` + `.cpp` | 输入布局（顶点数据→着色器输入的映射） |
| `Source/DX3D/Graphics/GraphicsPipelineState.h` + `.cpp` | 整体渲染管线状态对象 |

**4c · 缓冲与纹理**
| 文件 | 读懂它你就知道了 |
|------|------|
| `Source/DX3D/Graphics/VertexBuffer.h` + `.cpp` | 顶点缓冲 |
| `Source/DX3D/Graphics/IndexBuffer.h` + `.cpp` | 索引缓冲 |
| `Source/DX3D/Graphics/ConstantBuffer.h` + `.cpp` | 常量缓冲（CPU 写、GPU 读，传矩阵/光照） |
| `Source/DX3D/Graphics/Texture.h` + `.cpp` | 纹理 + 着色器资源视图 |
| `Source/DX3D/Graphics/Sampler.h` + `.cpp` | 采样器（纹理过滤方式） |

### 阶段 5 · 游戏框架与 ECS
| 文件 | 读懂它你就知道了 |
|------|------|
| `DX3D/Include/DX3D/Game/Game.h` + `Source/.../Game/Game.cpp` | 游戏主类、主循环、deltaTime |
| `DX3D/Source/DX3D/Game/Win32/Win32Game.cpp` | run() 的 Win32 消息循环实现 |
| `DX3D/Include/DX3D/Game/Component.h` + `Source/.../Game/Component.cpp` | 组件基类 |
| `DX3D/Include/DX3D/Game/GameObject.h` + `Source/.../Game/GameObject.cpp` | 实体：按类型存组件、生命周期钩子 |
| `DX3D/Include/DX3D/Game/World.h` + `Source/.../Game/World.cpp` | 世界：管理所有对象/组件、延迟创建、脏标记 |
| `DX3D/Include/DX3D/Game/Display.h` + `Source/.../Game/Display.cpp` | 窗口+交换链的组合 |

### 阶段 6 · 资源系统
| 文件 | 读懂它你就知道了 |
|------|------|
| `DX3D/Include/DX3D/Resource/Resource.h` + `Source/.../Resource/Resource.cpp` | 资源基类（从文件加载） |
| `DX3D/Include/DX3D/Resource/ResourceManager.h` + `Source/.../Resource/ResourceManager.cpp` | 按路径缓存去重创建资源 |
| `DX3D/Include/DX3D/Resource/MeshResource.h` + `.cpp` | 用 tinyobjloader 解析 .obj 网格 |
| `DX3D/Include/DX3D/Resource/TextureResource.h` + `.cpp` | 用 stb_image 读图片 |
| `DX3D/Include/DX3D/Resource/MaterialResource.h` + `.cpp` | 材质 = 着色器 + 参数 + 纹理 |

### 阶段 7 · 游戏组件
| 文件 | 读懂它你就知道了 |
|------|------|
| `DX3D/Include/DX3D/Component/TransformComponent.h` + `.cpp` | ⭐最重要：位置/旋转/缩放、刚体 vs 仿射矩阵、脏标记 |
| `DX3D/Include/DX3D/Component/CameraComponent.h` + `.cpp` | 相机：view 矩阵、透视投影 |
| `DX3D/Include/DX3D/Component/CubeComponent.h` + `.cpp` | 程序化生成立方体 |
| `DX3D/Include/DX3D/Component/MeshComponent.h` + `.cpp` | 挂载 .obj 网格、多材质槽位 |
| `DX3D/Include/DX3D/Component/DirectionalLightComponent.h` + `.cpp` | 方向光（颜色/强度/方向） |

> 注：代码里这个类名实际拼写为 `DirectionaLightComponent`（少一个字母 l），文件名则是完整的 `DirectionalLightComponent`。看到别意外。

### 阶段 8 · 渲染器与着色器（把一切串起来）
| 文件 | 读懂它你就知道了 |
|------|------|
| `DX3D/Source/DX3D/Game/WorldRenderer.h` + `.cpp` | ⭐核心：遍历 ECS 组件，组装成 D3D 绘制命令 |
| `DX3D/Assets/Shaders/Common.hlsl` | 公共着色器：顶点变换、Phong 光照、VS/PS 钩子框架 |
| `Game/Assets/Shaders/BasicShader.hlsl` | 最简材质（纯白无纹理） |
| `Game/Assets/Shaders/MaterialShader.hlsl` | 带纹理的材质（采样贴图 + 高光） |

> 读完 `WorldRenderer.cpp` + `Common.hlsl`，你就把「数据怎么变成屏幕像素」整条链路打通了。

---

## 6. 关键概念速查（带文件指向）

| 概念 | 在哪看 | 一句话 |
|------|--------|--------|
| Desc 创建模式 | `Core/Common.h` | 所有 `*Desc` 结构体是创建对象的参数表 |
| typeid 反射 | `Core/Identifiable.h` | `dx3d_typeid` 宏给每个类一个唯一 ID，ESC 据此按类型查组件 |
| IsRegistered 约束 | `Core/Identifiable.h` | C++20 concept，限制只能创建/查注册过的类型 |
| 禁拷贝宏 | `Core/Core.h` | `dx3d_disable_copy_and_move` 删掉拷贝/移动构造赋值 |
| 日志宏 | `Core/Logger.h` | `DX3DLogInfo/Warning/Error/ThrowError` 一行打日志/抛异常 |
| 智能指针 | `Core/Core.h` | `RefPtr`=shared_ptr，`UniquePtr`=unique_ptr |
| COM 智能指针 | `Graphics/GraphicsDevice.h` | `Microsoft::WRL::ComPtr` 管理 DirectX COM 对象 |
| 设备 vs 上下文 | `Graphics/GraphicsDevice.*` `DeviceContext.*` | 设备建资源，上下文下命令 |
| 交换链/双缓冲 | `Graphics/SwapChain.*` | 画后台缓冲，present 交换到前台 |
| 常量缓冲 cbuffer | `Graphics/ConstantBuffer.*` + `Common.hlsl` | CPU 每帧写、GPU 着色器读的全局数据（相机/光照/物体矩阵） |
| 刚体 vs 仿射矩阵 | `Component/TransformComponent.*` | 顶点位置用仿射(含缩放)，法线用刚体(不含缩放)以免光照失真 |
| 脏标记延迟更新 | `Component/TransformComponent.*` `Game/World.*` | 改 Transform 只标记 dirty，要用时才算，World 还能批量更新 |
| 透视投影 | `Math/Mat4x4.h` `perspectiveFovLH` | 把 3D 透视压扁成屏幕的 2D（远小近大） |
| ECS 按类型存储 | `Game/World.h` | `unordered_map<typeId, vector<Component*>>` |
| 延迟创建 | `Game/World.*` | 遍历时不能改容器，新对象先存 pending，帧末统一提交 |
| Phong 光照 | `Common.hlsl` `ComputePhongDirectionalLight` | 最终色 = 环境光 + 漫反射(N·L) + 镜面高光(R·V)^shininess |
| 寄存器绑定 | `Common.hlsl` | b0/b1/b2/b3=常量缓冲，t0=纹理，s0=采样器，与 C++ 端 set* 对应 |

---

## 7. 如何构建与运行

**前提**：Windows 10+、Visual Studio 2022（带 C++ 桌面开发与 DirectX 组件）。

1. 双击打开仓库根目录的 `DirectXGame.sln`。
2. 顶部选 `Debug` 或 `Release`、`x64`。
3. 点工具栏 **「本地 Windows 调试器」**（或按 F5）编译并运行。
4. 运行后：WASD 移动、鼠标转视角（鼠标被锁定隐藏），看到一个旋转的方向光照射下的立方体台座与大理石半身像。

> 在 macOS/Linux 上无法编译（DirectX + Win32 仅限 Windows）。

---

## 8. 阅读建议

1. **先读阶段 0**（`Game/`），建立「这游戏在干什么」的直觉，再往下挖引擎实现。
2. **跟着数据走**：读 `WorldRenderer.cpp` 时，对照 `Common.hlsl` 看「C++ 这边设置的数据，HLSL 那边怎么用」，一帧的渲染链路就通了。
3. **善用 `#include` 反向追溯**：不懂某个类从哪来，看它的头文件 `#include` 了什么、被谁 `#include`。
4. **对照教程标签**：仓库的 [Tags](https://github.com/PardCode/CPP-3D-Game-Tutorial-Series/tags) 与 [YouTube 播放列表](https://www.youtube.com/playlist?list=PLv8DnRaQOs5-ST_VDqgbbMRtzMtpK36Hy) 按教程推进，每个 tag 对应一节课的代码状态，可逐步对照。
5. **遇到不懂的 C++ 语法**：`std::span`、`std::views::iota`、`concept`、`std::format`、`enable_shared_from_this` —— 这些都是 C++20 常见用法，注释里都有解释，先理解「它解决什么问题」再记语法。
6. **不要纠结 Vendor**：`DX3D/Vendor/` 下的 `stb_image.h`、`tiny_obj_loader.h` 是第三方库，读它们的调用方式即可，不必读内部实现。

---

## 9. 一张图总结一帧渲染

```
   CPU 端                                          GPU 端
   ───────                                         ──────
Game::onInternalUpdate()
  │
  ▼ deltaTime
InputSystem::update()        ── 采集键鼠
  │
  ▼
MainGame::onUpdate()         ── 游戏逻辑：玩家移动、灯旋转（改 Transform）
  │
  ▼
World::update()              ── 提交待创建对象、批量更新脏 Transform 矩阵
  │
  ▼
WorldRenderer::render()
  │  ├─ clearAndSetBackBuffer        ── 清后台缓冲为背景色
  │  ├─ 遍历 DirectionalLight ──┐
  │  ├─ 遍历 Camera ─────────────┼─► 更新 ConstantBuffer(b0物体/b1相机/b2环境/b3材质)
  │  ├─ 遍历 Cube / Mesh ────────┘    设置 VertexBuffer / IndexBuffer / Texture(t0) / Sampler(s0)
  │  └─ drawIndexedTriangleList   ── 画三角形 ─► 顶点着色器(仿射矩阵变换位置/刚体矩阵变换法线)
  │                                                  → 像素着色器(采样纹理 + Phong光照)
  │                                                  → 写入后台缓冲
  ▼
GraphicsDevice::executeCommandList()  ── 提交命令
  │
  ▼
SwapChain::present()        ── 交换前后台缓冲 ─► 屏幕显示新画面
```

祝你读得顺利。有任何一块看不懂，先回到本指南对应章节，再去看那个文件的注释。
